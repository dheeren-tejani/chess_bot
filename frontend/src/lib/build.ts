/** Bump this string every time you deploy. It shows on the Home screen,
    the loading screen, and in the ?debug overlay — so one glance tells
    you whether the live site is running the code you just pushed. */
export const BUILD_ID = 'v7';